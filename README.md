# my-anime - Isekai Virtual Space

## 実装内容
- `Assets/Scripts/WorldBuilder.cs`
  - Unityシーンで異世界仮想空間を自動生成するスクリプト
- `Assets/Editor/WorldBuilderMenu.cs`
  - Unityエディタ上のメニューから空間を生成できる
- `.gitignore`
  - Unityプロジェクト用の一般的な無視設定を追加

## 使い方
1. Unity 6.0.4.8f1 でこのプロジェクトを開きます。
2. `Assets/MainScene.unity` を開きます。
3. Unityメニューから `Tools > Isekai Virtual World > Create Virtual Space` を選択します。
4. シーンに `Isekai World Builder` が生成され、異世界空間の環境が構築されます。
5. シーンを保存して `Play` を押します。

## 注意点
- 現在、このフォルダ `/Users/tsuyosiito/my-anime` には Git リポジトリの `.git` フォルダが見つかりませんでした。
- GitHub と連携したい場合は、このフォルダで `git init` してからコミットしてください。
